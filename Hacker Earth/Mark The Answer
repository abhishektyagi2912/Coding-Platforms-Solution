#include <iostream>
using namespace std;
int main() {
	int n,diff;
	cin>>n>>diff;
	int arr[n];
	int ans=0,flag=0;
	for(int i=0;i<n;i++){
		cin>>arr[i];
		if(arr[i]<=diff) ans++;
		else flag++;
		if(flag>1) break;
	}
	cout<<ans;
	return 0;
}
