class Solution {
public:
    string restoreString(string s, vector<int>& indices) {
        string a=s;
        for(int i=0;i<indices.size();i++){
            int val = indices[i];
            a[val]=s[i];
        }
        return a;
    }
};
