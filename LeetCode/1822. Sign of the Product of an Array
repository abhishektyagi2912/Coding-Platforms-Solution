class Solution {
public:
    int arraySign(vector<int>& nums) {
        int sum =1;
        for(int x:nums){
            if(x>0) sum*=1;
            else if(x<0) sum*=-1;
            else return 0;
        }
        return sum;
    }
};
