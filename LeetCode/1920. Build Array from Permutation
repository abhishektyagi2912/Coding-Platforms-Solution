class Solution {
public:
    vector<int> buildArray(vector<int>& nums) {
        int n=nums.size();
        vector<int>r(n);
        for(int i=0;i<nums.size();i++){
            r[i]=nums[nums[i]];
        }
        return r;
    }
};
