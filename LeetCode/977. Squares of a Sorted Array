class Solution {
public:
    vector<int> sortedSquares(vector<int>& nums) {
        vector<int> c(nums.size());
        for(int i=0;i<nums.size();i++){
            c[i]=nums[i]*nums[i];
        }
        sort(c.begin(),c.end());
        return c;
        
    }
};
